--- /home/todoriri/SRE/mobile_game/space-shooter/src/game/systems/RenderingSystem.tsx	2026-02-13 18:58:33.444911104 +0100
+++ /1/Projects/space-shooter/src/game/systems/RenderingSystem.tsx	2026-02-13 18:41:27.227205873 +0100
@@ -43,6 +43,8 @@
 export const RenderingSystem = (entities: Record<string, GameEntity>) => {
   const renderables: JSX.Element[] = [];
 
+  if (!entities) return renderables;
+
   Object.keys(entities).forEach(id => {
     const entity = entities[id];
 
@@ -69,7 +71,7 @@
       height: position.height || 0,
       backgroundColor: renderable.color,
       opacity: renderable.alpha,
-      borderRadius: entity.components.collider?.type === 'circle' ? '50%' : 0,
+      borderRadius: entity.components.collider?.type === 'circle' ? 999 : 0,
       transform: [
         { rotate: `${position.rotation || 0}deg` },
         { scale: renderable.scale || 1 },
@@ -79,11 +81,12 @@
 
     // Add glow effect if enabled
     if (renderable.glowEffect) {
-      entityStyle['shadowColor'] = renderable.color;
-      entityStyle['shadowOffset'] = { width: 0, height: 0 };
-      entityStyle['shadowOpacity'] = 0.8;
-      entityStyle['shadowRadius'] = 10;
-      entityStyle['elevation'] = 10; // For Android
+      const style = entityStyle as any;
+      style.shadowColor = renderable.color;
+      style.shadowOffset = { width: 0, height: 0 };
+      style.shadowOpacity = 0.8;
+      style.shadowRadius = 10;
+      style.elevation = 10; // For Android
     }
 
     // Add pulse animation if enabled
@@ -227,16 +230,16 @@
   switch (bulletType) {
     case BulletType.PLAYER:
       style.backgroundColor = '#4FC3F7'; // Light blue
-      style.borderRadius = '50%';
+      style.borderRadius = 999;
       break;
     case BulletType.ENEMY:
       style.backgroundColor = '#FF8A65'; // Light red
-      style.borderRadius = '50%';
+      style.borderRadius = 999;
       style.transform = [{ rotate: '180deg' }]; // Pointing downward
       break;
     case BulletType.POWER_UP:
       style.backgroundColor = '#BA68C8'; // Purple
-      style.borderRadius = '50%';
+      style.borderRadius = 999;
       style.shadowColor = '#BA68C8';
       style.shadowRadius = 15;
       style.elevation = 15;
@@ -302,7 +305,7 @@
   }
 
   // Power-up icon (simple geometric shape)
-  let iconStyle = styles.powerUpIcon;
+  let iconStyle: any = styles.powerUpIcon;
   switch (powerUpType) {
     case PowerUpType.SHIELD:
       iconStyle = styles.shieldIcon;
@@ -384,7 +387,7 @@
     position: 'absolute',
     width: '120%',
     height: '120%',
-    borderRadius: '50%',
+    borderRadius: 999,
     borderWidth: 2,
     borderColor: '#29B6F6',
     opacity: 0.5,
@@ -435,7 +438,7 @@
   powerUp: {
     justifyContent: 'center',
     alignItems: 'center',
-    borderRadius: '50%',
+    borderRadius: 999,
   },
   powerUpIcon: {
     width: '50%',
@@ -447,7 +450,7 @@
     width: '60%',
     height: '60%',
     backgroundColor: '#FFFFFF',
-    borderRadius: '50%',
+    borderRadius: 999,
     borderWidth: 2,
     borderColor: '#29B6F6',
   },
@@ -461,14 +464,14 @@
     width: '50%',
     height: '50%',
     backgroundColor: '#FFFFFF',
-    borderRadius: '50%',
+    borderRadius: 999,
     position: 'relative',
   },
   bombIcon: {
     width: '50%',
     height: '50%',
     backgroundColor: '#FFFFFF',
-    borderRadius: '50%',
+    borderRadius: 999,
   },
   healthIcon: {
     width: '50%',
@@ -480,7 +483,7 @@
     width: '50%',
     height: '50%',
     backgroundColor: '#FFFFFF',
-    borderRadius: '50%',
+    borderRadius: 999,
   },
 
   // Generic entity style
